# OpenAI API key for the AI geometric interpretation feature.
# Set this as an environment variable in your Vercel project settings.
# Do NOT prefix with VITE_ — this is used server-side only.
OPENAI_API_KEY=sk-...
